name: Update meta-gbp

on:
  workflow_call:
    inputs:
      dists-json:
        required: true
        type: string
      arches-json:
        required: false
        type: string
        default: '["amd64"]'
      publish:
        type: boolean
        default: true
    secrets:
      DEPLOY_SSH_KEY:
        required: false

permissions:
  contents: write
  packages: read

jobs:
  update:
    runs-on: ubuntu-latest
    env:
      DEBFULLNAME: ${{ vars.DEBFULLNAME || 'Luis Alejandro Martínez Faneyth' }}
      DEBEMAIL: ${{ vars.DEBEMAIL || 'luis@luisalejandro.org' }}
    outputs:
      rev: ${{ steps.rev.outputs.rev }}
      py: ${{ steps.rev.outputs.py }}
      py_repo_dir: ${{ steps.rev.outputs.py_repo_dir }}
    steps:
      - name: Checkout py repo
        uses: actions/checkout@v7
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Checkout python-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/python-pipeline
          path: python-pipeline

      - name: Configure git
        run: |
          git config --global user.name "${DEBFULLNAME}"
          git config --global user.email "${DEBEMAIL}"

      - name: Setup workspace
        run: |
          bash python-pipeline/scripts/ci-setup-workspace.sh \
            "$GITHUB_WORKSPACE" \
            "$GITHUB_WORKSPACE/python-pipeline"

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull builder images
        env:
          PIPELINE_DIR: ${{ github.workspace }}/python-pipeline
          WORKSPACE: ${{ github.workspace }}
          DOCKERSHELF_SUITES: ''
        run: bash python-pipeline/scripts/ci-pull-builder-images.sh

      - name: meta-gbp update
        run: python-pipeline/meta-gbp update --no-interactive

      - name: Export revision
        id: rev
        run: |
          echo "rev=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "py=$(echo '${{ github.repository }}' | cut -d/ -f2 | sed 's/^py//')" >> "$GITHUB_OUTPUT"
          echo "py_repo_dir=$GITHUB_WORKSPACE" >> "$GITHUB_OUTPUT"

      - name: Push update commits
        run: |
          git remote set-url origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
          if git rev-parse origin/main >/dev/null 2>&1; then
            ahead="$(git rev-list --count origin/main..HEAD || true)"
          else
            ahead="$(git rev-list --count HEAD || true)"
          fi
          if [[ "${ahead}" != "0" ]]; then
            git push origin HEAD:main
          fi

  build:
    needs: update
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    env:
      DEBFULLNAME: ${{ vars.DEBFULLNAME || 'Luis Alejandro Martínez Faneyth' }}
      DEBEMAIL: ${{ vars.DEBEMAIL || 'luis@luisalejandro.org' }}
      DEB_BUILD_OPTIONS: noautodbgsym parallel=2
      DOCKERSHELF_ARCH: ${{ matrix.arch }}
    timeout-minutes: 360
    strategy:
      fail-fast: false
      matrix:
        dist: ${{ fromJSON(inputs.dists-json) }}
        arch: ${{ fromJSON(inputs.arches-json) }}
    steps:
      - name: Checkout py repo
        uses: actions/checkout@v7
        with:
          repository: ${{ github.repository }}
          ref: ${{ needs.update.outputs.rev }}
          submodules: recursive
          fetch-depth: 0

      - name: Checkout python-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/python-pipeline
          path: python-pipeline

      - name: Configure git
        run: |
          git config --global user.name "${DEBFULLNAME}"
          git config --global user.email "${DEBEMAIL}"

      - name: Setup workspace
        run: |
          bash python-pipeline/scripts/ci-setup-workspace.sh \
            "$GITHUB_WORKSPACE" \
            "$GITHUB_WORKSPACE/python-pipeline"

      - name: Check if already published
        id: published
        run: bash python-pipeline/scripts/ci-check-published.sh "${{ matrix.dist }}" "${{ matrix.arch }}"

      - name: Free runner disk space
        if: steps.published.outputs.skip != 'true'
        run: |
          echo "Before cleanup:"
          df -h /
          sudo rm -rf \
            /usr/local/lib/android \
            /usr/share/dotnet \
            /usr/share/swift \
            /opt/ghc \
            /usr/local/.ghcup \
            /usr/local/share/powershell \
            /usr/local/share/chromium \
            /opt/hostedtoolcache
          sudo apt-get clean
          docker system prune -af || true
          echo "After cleanup:"
          df -h /

      - name: Log in to GHCR
        if: steps.published.outputs.skip != 'true'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull builder images
        if: steps.published.outputs.skip != 'true'
        env:
          PIPELINE_DIR: ${{ github.workspace }}/python-pipeline
          WORKSPACE: ${{ github.workspace }}
          DOCKERSHELF_SUITES: ${{ matrix.dist }}
        run: bash python-pipeline/scripts/ci-pull-builder-images.sh

      - name: Materialize and build
        if: steps.published.outputs.skip != 'true'
        run: |
          python-pipeline/meta-gbp materialize "${{ matrix.dist }}"
          python-pipeline/meta-gbp build

      - name: Upload debs
        if: steps.published.outputs.skip != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: debs-${{ matrix.dist }}-${{ matrix.arch }}
          path: python-pipeline/dist/*.deb
          if-no-files-found: error

  smoke:
    needs: [build, update]
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    env:
      DOCKERSHELF_ARCH: ${{ matrix.arch }}
    strategy:
      fail-fast: false
      matrix:
        dist: ${{ fromJSON(inputs.dists-json) }}
        arch: ${{ fromJSON(inputs.arches-json) }}
    steps:
      - name: Checkout packaging repo
        uses: actions/checkout@v7
        with:
          repository: ${{ github.repository }}
          ref: ${{ needs.update.outputs.rev }}
          path: packaging-repo
          sparse-checkout: |
            changelogs
            debiandirs

      - name: Checkout python-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/python-pipeline
          path: python-pipeline
          sparse-checkout: |
            scripts

      - name: Check if already published
        id: published
        working-directory: packaging-repo
        run: |
          bash "$GITHUB_WORKSPACE/python-pipeline/scripts/ci-check-published.sh" \
            "${{ matrix.dist }}" "${{ matrix.arch }}"

      - name: Download debs
        if: steps.published.outputs.skip != 'true'
        uses: actions/download-artifact@v8
        with:
          name: debs-${{ matrix.dist }}-${{ matrix.arch }}
          path: dist

      - name: Smoke test
        if: steps.published.outputs.skip != 'true'
        run: |
          bash python-pipeline/scripts/debian-smoke-test.sh \
            --dist "${{ matrix.dist }}" \
            --py "${{ needs.update.outputs.py }}" \
            --dist-dir dist \
            --arch "${{ matrix.arch }}"

  publish:
    concurrency:
      group: dockershelf-apt-publish
      cancel-in-progress: false
    needs: [smoke, update]
    if: inputs.publish && vars.DEPLOY_HOST != ''
    runs-on: ubuntu-latest
    env:
      DOCKERSHELF_ARCH: ${{ matrix.arch }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        dist: ${{ fromJSON(inputs.dists-json) }}
        arch: ${{ fromJSON(inputs.arches-json) }}
    steps:
      - name: Checkout packaging repo
        uses: actions/checkout@v7
        with:
          repository: ${{ github.repository }}
          ref: ${{ needs.update.outputs.rev }}
          path: packaging-repo
          sparse-checkout: |
            changelogs
            debiandirs

      - name: Checkout python-pipeline
        uses: actions/checkout@v7
        with:
          repository: Dockershelf/python-pipeline
          path: python-pipeline
          sparse-checkout: |
            scripts
            debian-repo-setup

      - name: Check if already published
        id: published
        working-directory: packaging-repo
        run: |
          bash "$GITHUB_WORKSPACE/python-pipeline/scripts/ci-check-published.sh" \
            "${{ matrix.dist }}" "${{ matrix.arch }}"

      - name: Download debs
        if: steps.published.outputs.skip != 'true'
        uses: actions/download-artifact@v8
        with:
          name: debs-${{ matrix.dist }}-${{ matrix.arch }}
          path: dist

      - name: Deploy preflight
        if: steps.published.outputs.skip != 'true'
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
          DEPLOY_INCOMING: ${{ vars.DEPLOY_INCOMING }}
        run: bash python-pipeline/scripts/ci-deploy-preflight.sh

      - name: Setup SSH
        if: steps.published.outputs.skip != 'true'
        uses: webfactory/ssh-agent@v0.10.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Publish to APT droplet
        if: steps.published.outputs.skip != 'true'
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_USER: ${{ vars.DEPLOY_USER }}
          DEPLOY_DIR: ${{ vars.DEPLOY_DIR }}
          DEPLOY_INCOMING: ${{ vars.DEPLOY_INCOMING }}
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts
          bash python-pipeline/scripts/ci-publish.sh "${{ matrix.dist }}" dist "${{ matrix.arch }}"
